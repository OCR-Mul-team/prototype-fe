# AutoScan Sell 서비스 성과 보고서

## 프로젝트 개요

**AutoScan Sell**은 현대자동차 인증중고차 매입 서비스를 위한 OCR 기반 문서 처리 및 실시간 고객-상담원 연동 시스템입니다.

---

## 주요 성과

### 1. 실시간 양방향 통신 시스템 구축

**Socket.IO 기반 실시간 연동**
- 고객과 상담원 간 실시간 세션 동기화
- 문서 업로드, OCR 처리 상태, 가격 예측 결과가 즉시 양측에 반영
- 연결 끊김 시 자동 재연결 (최대 5회 시도)
- 세션 기반 관리로 여러 고객 동시 처리 가능

**이벤트 흐름**
```
고객 → 서버: 세션 참가, 문서 업로드, 차량 정보 제출
서버 → 고객: OCR 처리 완료, 추가 서류 요청, 가격 보고서
서버 → 상담원: 새 세션 알림, 문서 업로드 알림, OCR 결과
```

---

### 2. OCR 기반 문서 자동 분류 및 검증

**지원 서류 유형**
| 서류 유형 | 설명 |
|----------|------|
| 자동차등록증 (vehicle_registration) | 차량번호, 차종, 연식, 소유자, 차대번호 추출 |
| 자동차세 납부증명 (tax_payment) | 납세자, 차량번호, 납부금액 추출 |
| 인감증명서 (seal_certificate) | 성명, 주소, 용도 추출 |
| 사업자등록증 (business_registration) | 상호, 대표자, 사업자등록번호 추출 |

**문서 검증 로직**
- 발급일 기준 30일 유효기간 자동 검증
- 서류 타입별 필수 필드 검증 (차량번호, 소유자 등)
- 검증 결과에 따른 상태 분류: `valid`, `needs_review`, `processing`

---

### 3. 서류 간 일관성 자동 검증

**Cross-Document Validation**
- 여러 서류에서 추출된 차량번호 일치 여부 확인
- 소유자/납세자/대표자 이름 일관성 검증
- 불일치 발견 시 상담원과 고객에게 실시간 경고

```typescript
// 검증 예시
서류 1 (자동차등록증): 차량번호 "12가 3456", 소유자 "홍길동"
서류 2 (납세증명서): 차량번호 "12가 3456", 납세자 "홍길동"
→ 일관성 검증 통과

서류 1: 소유자 "홍길동"
서류 2: 납세자 "김철수"
→ 경고: "서류 간 소유자 이름이 다릅니다"
```

---

### 4. AI 가격 예측 연동

**AWS Lambda 기반 가격 예측 API 연동**
- 차량 정보 입력 기반 예측 가격 산출
- 예측 범위 (최소/최대) 제공
- 신뢰도 점수 표시

**입력 항목**
- 브랜드, 모델명, 등급
- 배기량, 연식, 주행거리
- 연료 타입, 색상
- 신차가격, 차령(개월)

---

### 5. OCR 데이터 자동 입력

**폼 자동 완성 기능**
- OCR로 인식된 정보를 차량 정보 폼에 자동 입력
- 자동 입력된 필드는 녹색 테두리로 시각적 구분
- 미입력 필드는 사용자가 직접 입력하도록 안내

**추출 가능 정보**
- 브랜드 (제네시스, 현대, 기아 등)
- 배기량 (cc 단위)
- 연식 (년도)
- 연료 타입 (가솔린, 디젤, 하이브리드, 전기)
- 색상 (white, black, silver, blue, red, gray)

---

### 6. PDF 보고서 생성

**html2canvas + jsPDF 기반**
- 가격 예측 결과를 PDF로 즉시 다운로드
- 고객에게 공유 가능한 공식 보고서 형태

---

### 7. 고객 경험 개선

**개선 사항**
- 고객 이름 입력으로 상담원이 "OOO 고객님"으로 호칭 가능
- OCR 완료 후 "상담원 연결" 버튼 실시간 표시
- 전화번호 탭-투-콜 지원

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | React 18, TypeScript, Vite, Tailwind CSS |
| State Management | Zustand |
| Real-time | Socket.IO Client |
| Backend | Node.js, Express, Socket.IO |
| OCR | Modal Serverless API |
| Price Prediction | AWS Lambda |
| PDF Generation | html2canvas, jsPDF |

---

## 아키텍처

```
┌─────────────────┐     Socket.IO      ┌──────────────────┐
│   고객 페이지    │◄──────────────────►│   백엔드 서버     │
│  (CustomerPage) │                    │   (Express +     │
└─────────────────┘                    │    Socket.IO)    │
                                       └────────┬─────────┘
┌─────────────────┐     Socket.IO              │
│  상담원 대시보드  │◄────────────────────────────┤
│ (AgentDashboard)│                            │
└─────────────────┘                            │
                                               ▼
                              ┌────────────────┴────────────────┐
                              │                                 │
                        ┌─────▼─────┐                   ┌───────▼───────┐
                        │  OCR API  │                   │ Price API     │
                        │  (Modal)  │                   │ (AWS Lambda)  │
                        └───────────┘                   └───────────────┘
```

---

## 향후 개선 사항

1. **OCR API 안정화**: Modal 서버리스 API 상시 가동 또는 대체 서비스 검토
2. **가격 예측 API 정확도 향상**: 입력 파라미터 스펙 최적화
3. **추가 서류 유형 지원**: 보험 증권, 정비 이력 등
4. **다국어 지원**: 영어, 중국어 등 글로벌 확장
5. **모바일 최적화**: 카메라 촬영 및 실시간 OCR

---

## 정량적 성과 (예상)

| 지표 | 기존 방식 | AutoScan Sell | 개선율 |
|------|----------|---------------|--------|
| 서류 확인 시간 | 수동 10분 | 자동 2초 | 99.7%↓ |
| 데이터 입력 시간 | 수동 5분 | 자동 입력 | 95%↓ |
| 서류 오류 발견 | 육안 검토 | 실시간 자동 | 100% 자동화 |
| 고객 대기 시간 | 상담원 배정 대기 | 실시간 연결 | 즉시 |
